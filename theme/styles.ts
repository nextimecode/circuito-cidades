import { Dict } from '@chakra-ui/utils'

const styles = {
  global: (props: Dict<any>) => ({
    body: {
      fontFamily: 'Inter',
      lineHeight: 'base'
    },
    h1: {
      color: 'primary',
      fontWeight: 700
    }
  })
}

export default styles
